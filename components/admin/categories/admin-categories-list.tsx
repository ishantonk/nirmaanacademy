import {
    Card,
    CardContent,
    CardDescription,
    CardHeader,
    CardTitle,
} from "@/components/ui/card";

export function AdminCategoriesList() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Categories</CardTitle>
                <CardDescription>List of all the categories.</CardDescription>
            </CardHeader>
            <CardContent></CardContent>
        </Card>
    );
}
